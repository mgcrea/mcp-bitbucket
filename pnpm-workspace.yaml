allowBuilds:
  esbuild: true

# Local development override.
#
# `dependencies` declares the real `^0.5.0`, which is what gets published. This points
# the install at the sibling checkout until bitbucket-cli 0.5.0 is on npm — the OAuth
# strategy and the commit/ref/source paths this server needs landed there but are not
# released yet. Delete this block once it is published.
overrides:
  "@mgcrea/bitbucket-cli": "link:../../../../Developer/github/bitbucket-cli"
